/**
 * Theme — backward-compatible re-export from the design token system.
 *
 * New code should import from "@/design" directly.
 * Existing imports from "@/theme" continue to work unchanged.
 */
export { colors, typography, spacing, radius, MIN_TAP_TARGET } from "@/design/tokens";
export { shadows } from "@/design/shadows";
